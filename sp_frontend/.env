# Server Configuration
VITE_HOST=0.0.0.0
VITE_PORT=5173
VITE_API_URL=http://localhost:6000

# For production deployment, you can override these:
# VITE_HOST=your-server-ip
# VITE_API_URL=http://your-backend-server:6000

# Production environment variables (automatically used when NODE_ENV=production)
# VITE_API_URL will be set to the same domain as the frontend in production